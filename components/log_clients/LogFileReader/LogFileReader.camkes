/* Copyright (C) 2020, HENSOLDT Cyber GmbH */

/**
 * @file
 * @brief   Log file reader used at the end of the demo to dump logs from the
 *          files to the console.
 */

/** @cond SKIP_IMPORTS */
import <if_OS_Logger.camkes>;
/** @endcond */

/** @hideinitializer */
component LogFileReader {
    control;

    // interface to the log server
    dataport Buf                dataport_buf;

    consumes DataAvailableEvent logServer_finish;

    uses     if_OS_Logger       log_server_interface;
}
