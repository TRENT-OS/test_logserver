import <if_ChanMux.camkes>;
import <if_OS_PartitionManager.camkes>;

#include "ChanMux/ChanMuxHelper.camkes"

component PartitionManager {

    //-------------------------------------------------
    // upper interface
    provides  partition_manager_interface  api_pm_component;
    dataport  Buf                          logServer_port;
    dataport  Buf                          fileReaderWriter_port;

    //-------------------------------------------------
    // lower interface to ChanMux
    DECLARE_CHANMUX_INTERFACE_USER(chanMux)
    DECLARE_CHANMUX_CHANNEL_CONNECTOR(chanMux, chan)
}
